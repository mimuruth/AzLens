{
  "name": "mcp-github",
  "version": "1.0.0",
  "description": "MCP server for GitHub repositories, issues, pull requests, and code",
  "type": "module",
  "main": "build/index.js",
  "bin": {
    "mcp-github": "build/index.js"
  },
  "files": [
    "build"
  ],
  "scripts": {
    "build": "tsc && node -e \"import('fs').then(fs => fs.chmodSync('build/index.js', 0o755))\"",
    "watch": "tsc --watch",
    "start": "node build/index.js",
    "start:http": "node build/http.js",
    "dev": "tsc && node build/index.js",
    "clean": "node -e \"import('fs').then(fs => fs.rmSync('build', { recursive: true, force: true }))\""
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "github"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@azure/monitor-opentelemetry": "^1.8.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^4.21.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^26.1.2",
    "typescript": "^5.6.3"
  }
}
